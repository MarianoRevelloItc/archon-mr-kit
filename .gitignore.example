# Add these lines to your project's .gitignore.
# IMPORTANT: only `.archon/mcp/` is ignored — workflows/ and commands/ MUST be committed.

# Archon — workflow definitions are committed; only ignore mcp/ (env vars / secrets)
.archon/mcp/
